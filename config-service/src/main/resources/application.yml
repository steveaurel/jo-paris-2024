server:
  port: 9999

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: http://gitlab.com/steveaurel/config-repo
          default-label: master
          clone-on-start: true


